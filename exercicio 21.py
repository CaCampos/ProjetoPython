lista = [3, 5, 10, 5, 3, 15, 99]

for numero in lista:
    if numero == numero
    print(numero)

for numero in lista:
	if lista.count(numero) > 1 and nova_lista.count(numero) == 0:
		print("O número {} está repetido.".format(numero))
		nova_lista.append(numero)
